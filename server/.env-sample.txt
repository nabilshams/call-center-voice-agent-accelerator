# .env
AZURE_VOICE_LIVE_API_KEY=<Find the key from your AI foundry resource>
AZURE_VOICE_LIVE_ENDPOINT=<Find the endpoint from your AI foundry resource>
VOICE_LIVE_MODEL=gpt-4o-mini
ACS_CONNECTION_STRING=<Find the connection string from your Communication Service resource>
ACS_DEV_TUNNEL=<Optional, only set it when you run local ACS test>

# Ambient Scenes (Optional)
# Adds realistic background audio to simulate real-world call environments
# Options: none (default/disabled), office, call_center
# You can also add custom audio files - see README for details
# Example: AMBIENT_PRESET=call_center
AMBIENT_PRESET=none

# Voice Live Transport (Optional)
# Options: websocket (default, original protocol), webrtc (lower latency, requires aiortc)
# WebRTC uses /voice-live/realtime/calls endpoint with RTP media tracks
VOICE_LIVE_TRANSPORT=websocket

# ============================================================================
# TRAVEL ORCHESTRATION (Foundry / Microsoft Agent Framework + Local Fallback)
# ============================================================================
# Modes:
# - foundry: Use Foundry workflow first; fallback to local orchestrator on errors
# - maf: Use Microsoft Agent Framework workflow first; fallback to local orchestrator on errors
# - maf-local: Use local MAF-style if/else workflow graph in this codebase
# - local: Use local orchestrator only
TRAVEL_ORCHESTRATOR_MODE=foundry

# Foundry workflow endpoint (AI Foundry API base URL)
# Example: https://<your-project>.services.ai.azure.com
FOUNDRY_WORKFLOW_ENDPOINT=<Foundry workflow API base endpoint>

# Foundry workflow invoke path relative to endpoint
# Example: api/agents/<workflow-or-agent-name>:invoke
FOUNDRY_WORKFLOW_PATH=<Foundry workflow invoke path>

# Optional: API key auth for Foundry workflow
# Leave empty if using Managed Identity / DefaultAzureCredential
FOUNDRY_API_KEY=

# Request timeout in seconds for Foundry workflow calls
FOUNDRY_WORKFLOW_TIMEOUT_SECONDS=25

# Microsoft Agent Framework workflow endpoint (API base URL)
# Example: https://<your-project>.services.ai.azure.com
MAF_WORKFLOW_ENDPOINT=<Microsoft Agent Framework workflow API base endpoint>

# Microsoft Agent Framework workflow invoke path relative to endpoint
# Example: api/agents/<maf-workflow-or-agent-name>:invoke
MAF_WORKFLOW_PATH=<Microsoft Agent Framework workflow invoke path>

# Optional: API key auth for Microsoft Agent Framework workflow
# Leave empty if using Managed Identity / DefaultAzureCredential
MAF_API_KEY=

# Request timeout in seconds for Microsoft Agent Framework workflow calls
MAF_WORKFLOW_TIMEOUT_SECONDS=25

# Native local MAF SDK orchestration settings (used when TRAVEL_ORCHESTRATOR_MODE=maf-local)
# Set to false to force deterministic fallback implementation.
MAF_NATIVE_SDK_ENABLED=true

# Foundry project endpoint used by Native MAF SDK client.
# Example: https://<your-foundry-service>.services.ai.azure.com/api/projects/<project-name>
MAF_PROJECT_ENDPOINT=<Microsoft Foundry project endpoint for Native MAF SDK>

# Model used by Native MAF SDK agents in maf-local mode
MAF_MODEL=gpt-4o-mini

# ============================================================================
# TRANSCRIPTION FEATURE (Separate from Voice Agent)
# ============================================================================
# These settings are used for the standalone transcription feature at /transcription
# The transcription feature uses Azure Speech Services directly, not Voice Live API

# Azure Speech Service credentials (required for transcription feature)
# Get these from your Azure Speech resource in the Azure Portal
AZURE_SPEECH_KEY=<Find the key from your Azure Speech resource>
AZURE_SPEECH_REGION=<Region of your Azure Speech resource, e.g., eastus, westus2>

# Optional: Custom Speech endpoint (for custom speech models)
# AZURE_SPEECH_ENDPOINT=<Custom endpoint if using custom speech models>